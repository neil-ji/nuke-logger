{
  "name": "nuke-logger",
  "version": "1.0.0",
  "description": "A logger utility, like nuclear power, small, simple and strong.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "node script/deletedir.js ../lib",
    "replace-alias": "node script/replacealias.js ../lib",
    "test": "npm run clean && npm run build && npm run replace-alias && node lib/test"
  },
  "keywords": [
    "javascript"
  ],
  "author": "Neil Ji",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@types/node": "^14.14.3",
    "typescript": "^4.0.3"
  }
}
